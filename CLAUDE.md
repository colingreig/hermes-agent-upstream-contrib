# Claude Code — Hermes Agent

Follow `AGENTS.md`. Governed mini-bundle work ships on `mini` via `ssh mini` and
the matching `install_*.py` before handoff (`machine-setup/mini-scripts/README.md`);
never leave "post-merge on mini" as the only deploy step.
