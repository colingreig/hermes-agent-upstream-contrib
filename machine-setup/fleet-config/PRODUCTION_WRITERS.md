<!-- Generated by machine-setup/render_production_writers.py; DO NOT EDIT. -->
# Production writers

This is a generated index of the machine-readable [`production_mutation_registry.json`](../production_mutation_registry.json). Edit that registry, then run `python3 machine-setup/render_production_writers.py`; CI checks that this file is current.

## Unknown-path guard

**Default:** `fail-closed`. A production mutation is unsupported unless its actor, target resource, lock or transaction boundary, and rollback surface are present in this registry. Callers must reject an unknown path before it writes.

**Status:** This inventory is authoritative now; runtime admission enforcement is intentionally deferred to the bounded follow-on tasks below.

| Follow-on task | Hook point | Purpose |
| --- | --- | --- |
| `86e2kmucr` | production mutation admission check at governed installer and deploy entry points | Reject a requested resource mutation that has no registry actor/resource mapping. |
| `86e2kmuct` | shared executor-dispatch lease admission | Enforce the one logical executor dispatch surface named by this registry. |

## Resources

| ID | Path | Concurrency model | Description |
| --- | --- | --- | --- |
| `fleet-config` | `~/.hermes/config.yaml and ~/.hermes/profiles/*` | single installer transaction with pre-write snapshots | Fleet overlay and managed profile configuration. |
| `cron-jobs` | `~/.hermes/cron/jobs.json and ~/.hermes/cron/.jobs.lock` | atomic replacement plus the cron jobs advisory lock | Cron definitions and scheduler lifecycle metadata. |
| `skills-policy` | `~/.hermes/skills/, ~/.hermes/profiles/*/skills/, and ~/.hermes/state/skill-pulls/` | installer transaction or named skill-pull/catalog locks | Curated skill trees, suppression policy, and skill-pull evidence. |
| `runtime-release` | `~/.hermes/releases/ and ~/.hermes/runtime-current` | single mini release-cut directory lock and atomic symlink swap | Immutable release directories, current-release pointer, cut receipts, and prior-release pointer. |
| `governed-mini-scripts` | `~/.hermes/scripts/ and ~/Library/LaunchAgents/` | release-cut serialization with per-file hash verification | Hash-pinned operational scripts, wrappers, and launchd configuration installed by the release cut. |
| `executor-admission` | `~/.hermes/state/executor-admission.sqlite3` | SQLite BEGIN IMMEDIATE transaction with fencing token | Fenced lease and pending-wake state shared by both executor cron jobs. |
| `session-db` | `~/.hermes/state.db (including WAL sidecars)` | SQLite WAL multi-writer tolerant; lifecycle contract follows task 86e2kmud2 | Hermes sessions and dashboard-visible conversation state. |
| `kanban-dispatcher` | `~/.hermes/kanban/kanban/.dispatcher.lock and board databases` | machine-global advisory dispatcher lock plus SQLite transactions | Gateway-owned Kanban dispatch and board state. |
| `clickup-remote-state` | `ClickUp tasks, comments, statuses, and dependency records` | ClickUp API conditional workflow and task-level evidence contract | Remote task lifecycle state changed by governed executor, validator, and lifecycle jobs. |
| `repo-worktrees` | `/Users/colingreig/dev/* worktrees and governed branch refs` | per-repository worktree/claim controls; scripts own their deletion safety checks | Repository worktrees and cleanup targets used by executor and maintenance jobs. |
| `clickup-workspace-cache` | `~/.hermes/state/clickup-map.json, ~/.hermes/state/clickup-workspace-map.md, and ~/brain/clickup-workspace-map.md` | script-local atomic replacement and refresh lock | Refreshed ClickUp topology cache and canonical human-readable workspace note. |
| `pr-pipeline-state` | `~/.hermes/state/ci-health-*.json and ~/.hermes/scripts/pr_pipeline/* state` | script-local atomic state files and pipeline-specific locks | PR pipeline health, review-poll, and validation state used by no-agent watchdogs. |
| `claim-ledger` | `~/.hermes/state/claim* and executor claim records` | claim-store transactional ownership rules | Stranded task claim and lease records reconciled by the claim reaper. |
| `email-delivery` | `Postmark delivery to colin@colingreig.com` | one cron invocation; Postmark sender owns delivery retry and receipts | The explicit fleet health digest delivery side effect. |
| `purelymail-poller-deploy` | `~/.hermes/deploy/purelymail-poller/ and ~/.hermes/deploy/purelymail-poller/.lock` | external deployer-held named lock | External ignite-email-infra deployment target and deployment lease. |

## Actors

| Actor | Class | Owner | Access | Governed entry points | Resources | Lock or transaction boundary | Rollback surface | Operational pointers |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `fleet-config-installer` — Fleet configuration installer | `conductor-ssh` | `hermes-agent` | `mutating` | `machine-setup/fleet-config/install_fleet_config.py` | `fleet-config`, `cron-jobs`, `skills-policy` | exclusive no-follow file lock and install transaction: `~/.hermes/.fleet-config-install.lock`. Verifies sources, snapshots every destination, writes atomically, and restores snapshots on failure. | `~/.hermes/logs/fleet-config-installs/<receipt>/destinations`. Installer rollback restores planned snapshots; operator may inspect the receipt before a targeted recovery. | Activation: `machine-setup/fleet-config/install_fleet_config.py` — `python3 install_fleet_config.py --home <home>`<br>Rollback: `machine-setup/fleet-config/install_fleet_config.py::_rollback` — `automatic snapshot rollback on an install failure; inspect its receipt before targeted recovery`<br>Verification: `machine-setup/mini-scripts/verify_governed_paths.py` — `python3 verify_governed_paths.py --home <home>` |
| `mini-release-cut` — Hermes Mini release cut | `conductor-ssh` | `hermes-agent` | `mutating` | `scripts/mini-release-cut.sh` | `runtime-release`, `governed-mini-scripts` | directory lease and atomic release pointer swap: `~/.hermes/releases/.mini-release-cut.lock`. Builds and validates a fresh release before atomically repointing runtime-current; serializes cuts, rollback, and prune. | `~/.hermes/releases/.previous and .mini-release-last-receipt.json`. mini-release-cut.sh --rollback repoints runtime-current to the verified previous release and restarts services. | Activation: `scripts/mini-release-cut.sh` — `mini-release-cut.sh --ref <sha-or-ref> --certified-sha <full-sha> --promotion-receipt-id <sha256>`<br>Rollback: `scripts/mini-release-cut.sh` — `mini-release-cut.sh --rollback`<br>Verification: `machine-setup/mini-scripts/verify_governed_paths.py` — `python3 verify_governed_paths.py --home <home>` |
| `fleet-outcomes-reconciler` — Fleet outcomes reconciler | `conductor-ssh` | `hermes-agent` | `mutating` | `machine-setup/mini-scripts/reconcile_fleet_outcomes.py` | `governed-mini-scripts`, `cron-jobs` | snapshot-backed reconciliation under the cron jobs advisory lock: `~/.hermes/releases/fleet-outcomes and ~/.hermes/cron/.jobs.lock`. Hash-verifies every declared source, atomically replaces only manifest targets, and restores the recorded snapshot if verification fails. | `~/.hermes/releases/fleet-outcomes/previous and snapshots/<sha256>.json`. reconcile_fleet_outcomes.py rollback restores the exact prior scripts, LaunchAgents, and cron document from its content-addressed snapshot. | Activation: `machine-setup/mini-scripts/reconcile_fleet_outcomes.py` — `python3 reconcile_fleet_outcomes.py install --home <home> --reload`<br>Rollback: `machine-setup/mini-scripts/reconcile_fleet_outcomes.py` — `python3 reconcile_fleet_outcomes.py rollback --home <home> --reload`<br>Verification: `machine-setup/mini-scripts/verify_governed_paths.py` — `python3 verify_governed_paths.py --home <home>` |
| `ignite-skills-sync` — Ignite skills synchronization | `conductor-ssh` | `hermes-agent` | `mutating` | `machine-setup/mini-scripts/ignite-skills-pull.sh`<br>Legacy name: `sync_ignite_skills_to_hermes.sh` | `skills-policy` | named skill pull and catalog update leases: `~/.hermes/state/skill-pulls/ignite-skills-live.lock and catalog-update.lock`. The script acquires both leases, validates canonical roots, records the catalog generation, and releases only matching tokens. | `/Users/colingreig/dev/ignite-skills-live Git checkout and catalog-generation receipt`. Failing pulls publish a failed generation; recovery is a reviewed fast-forward or repository rollback before the next pull. | — |
| `production-clickup-executor` — Unified ClickUp executor dispatch | `cron` | `hermes-agent` | `mutating` | `cron/executor_admission.py`<br>`cron/scheduler.py`<br>Cron IDs: `62714b869845`, `dcab830aa41c`<br>Logical dispatch: `production-clickup-executor` | `executor-admission`, `clickup-remote-state`, `repo-worktrees`, `cron-jobs` | fenced SQLite lease: `~/.hermes/state/executor-admission.sqlite3 / production-clickup-executor`. Both executor cron IDs enter one lease identity; BEGIN IMMEDIATE and monotonically increasing fencing tokens reject overlap. | `ClickUp evidence handoffs and executor lease history`. Release or recover a lease through the admission ledger; revert repository work by its governed commit/PR workflow, never by deleting shared state. | — |
| `hermes-pr-validator` — Hermes PR validator cron | `cron` | `hermes-agent` | `mutating` | `cron/scheduler.py`<br>`machine-setup/fleet-config/jobs.json`<br>Cron IDs: `5a76e290811d` | `clickup-remote-state`, `repo-worktrees`, `cron-jobs` | cron jobs advisory lock plus validator evidence gate: `~/.hermes/cron/.jobs.lock and ClickUp handoff proof contract`. Scheduler serializes persisted job metadata; validator status mutation requires its proof packet and finalize token. | `ClickUp validator comments and repository CI/PR evidence`. Correct a false verdict with a new evidence-backed validator action; no local state rewrite is treated as a rollback. | — |
| `clickup-lifecycle-cron` — ClickUp lifecycle reconciliation cron | `cron` | `hermes-agent` | `mutating` | `cron/scheduler.py`<br>`machine-setup/fleet-config/jobs.json`<br>Cron IDs: `777876d3eb16` | `clickup-remote-state`, `cron-jobs` | cron jobs advisory lock and ordered reconciliation pass: `~/.hermes/cron/.jobs.lock`. Scheduler serializes job metadata; the job runs closeout, stalled-task, staleness, then reconciliation in order. | `ClickUp task event/comment history`. Repair incorrect remote lifecycle state with a subsequent audited reconciliation, preserving history. | — |
| `clickup-poll-gate-cron` — ClickUp poll gate watchdog | `cron` | `hermes-agent` | `mutating` | `scripts/clickup_poll_gate.py`<br>`~/.hermes/scripts/clickup_poll_gate.py`<br>Cron IDs: `9dca144ff19b` | `clickup-remote-state`, `cron-jobs` | cron jobs advisory lock and gate-local idempotency: `~/.hermes/cron/.jobs.lock`. The no-agent scheduler serializes its metadata while the poll gate owns its ClickUp eligibility checks. | `ClickUp task event history and cron output`. Correct an incorrect gate action through a subsequent guarded ClickUp reconciliation. | — |
| `review-poll-gate-cron` — Review poll gate watchdog | `cron` | `hermes-agent` | `mutating` | `machine-setup/mini-scripts/review_poll_gate.py`<br>`~/.hermes/scripts/review_poll_gate.py`<br>Cron IDs: `8d3b1d53470d` | `pr-pipeline-state`, `cron-jobs` | cron jobs advisory lock and packaged review-poll guard: `~/.hermes/cron/.jobs.lock`. The compatibility entry point delegates to pr_pipeline.review_poll_gate.safe_main, which owns the guarded poll state transition. | `PR pipeline state and cron output`. A follow-up guarded poll reconciles the review state; inspect its output before any targeted pipeline recovery. | — |
| `clickup-review-sla-cron` — ClickUp review SLA watchdog | `cron` | `hermes-agent` | `mutating` | `~/.hermes/scripts/clickup_review_sla.py`<br>Cron IDs: `ad0ae6b717e2` | `clickup-remote-state`, `cron-jobs` | cron jobs advisory lock and SLA script guard: `~/.hermes/cron/.jobs.lock`. The governed deployed script is the authority for SLA eligibility and remote task follow-up. | `ClickUp task/comment history and cron output`. Correct an SLA action with an auditable subsequent ClickUp update; do not rewrite scheduler history. | — |
| `spend-meter-cron` — Provider spend meter watchdog | `cron` | `hermes-agent` | `read-only` | `machine-setup/mini-scripts/spend_meter.py`<br>`~/.hermes/scripts/spend_meter.py`<br>Cron IDs: `b0c4c5cc70c1` | `session-db`, `cron-jobs` | cron jobs advisory lock: `~/.hermes/cron/.jobs.lock`. The meter reads session and receipt data; scheduler metadata is serialized by the cron lock. | `Cron output and alert delivery`. The meter does not mutate Hermes resources; investigate a bad alert from its source records and rerun after correction. | — |
| `ci-health-watch-cron` — CI health and PR staleness watchdog | `cron` | `hermes-agent` | `mutating` | `machine-setup/mini-scripts/ci-health-watch-cron.py`<br>`~/.hermes/scripts/ci-health-watch-cron.py`<br>Cron IDs: `e835c614cfb2` | `pr-pipeline-state`, `cron-jobs` | atomic watchdog state and cron jobs advisory lock: `~/.hermes/state/ci-health-pr-staleness-last-run.json`. The wrapper atomically claims the daily staleness run and retains separate CI/fleet probe state. | `CI health state receipts and cron output`. Remove or restore only a verified stale state receipt, then let the guarded next run rebuild it. | — |
| `clickup-workspace-refresh-cron` — ClickUp workspace topology refresh | `cron` | `hermes-agent` | `mutating` | `machine-setup/mini-scripts/clickup_workspace_refresh.py`<br>`~/.hermes/scripts/clickup_workspace_refresh.py`<br>Cron IDs: `bcf275768661` | `clickup-workspace-cache`, `cron-jobs` | refresh lock and atomic cache replacement: `~/.hermes/state/clickup-workspace-refresh.lock`. The refresher derives cache cadence from the registered cron and atomically replaces map and note artifacts. | `~/.hermes/state/clickup-workspace-map.prev.md`. Restore the previous verified markdown mirror if needed, then rerun a successful refresh to rebuild canonical cache state. | — |
| `reap-stranded-claims-cron` — Stranded claim reaper | `cron` | `hermes-agent` | `mutating` | `~/.hermes/scripts/reap_stranded_claims.py`<br>Cron IDs: `dd73a5e578e4` | `claim-ledger`, `cron-jobs` | claim-store ownership transaction and cron jobs advisory lock: `~/.hermes/state/claim*`. The deployed claim reaper owns stale-claim eligibility and releases only leases its ledger can prove stranded. | `Claim ledger history and cron output`. Recover an incorrectly released claim through the ledger's normal reassignment flow rather than rewriting prior claim records. | — |
| `ignite-board-sync-cron` — Ignite board synchronization | `cron` | `hermes-agent` | `mutating` | `~/.hermes/scripts/ignite-board-sync.sh`<br>Cron IDs: `542fca8d839f` | `clickup-remote-state`, `cron-jobs` | cron jobs advisory lock and board-sync idempotency: `~/.hermes/cron/.jobs.lock`. The deployed board-sync script owns its remote reconciliation and must remain idempotent across retries. | `ClickUp sync history and cron output`. Correct remote drift through a subsequent guarded synchronization, preserving task history. | — |
| `clickup-closeout-actor-cron` — ClickUp closeout actor | `cron` | `hermes-agent` | `mutating` | `machine-setup/mini-scripts/closeout_actor.py`<br>`~/.hermes/scripts/closeout_actor.py`<br>Cron IDs: `2ff001bea4b5` | `clickup-remote-state`, `pr-pipeline-state`, `cron-jobs` | guarded ClickUp status transition and cron jobs advisory lock: `~/.hermes/cron/.jobs.lock and clickup_status_guard.py`. The closeout actor requires merged PR and validator PASS evidence and routes status changes through the guarded ClickUp path. | `Validator verdict store and ClickUp task history`. Repair an incorrect review transition through the guarded status flow; complete remains unavailable to this actor. | — |
| `fleet-health-digest-cron` — Fleet health digest cron | `cron` | `hermes-agent` | `read-only` | `cron/scheduler.py`<br>`machine-setup/fleet-config/jobs.json`<br>Cron IDs: `f23a03e9d1b2` | `email-delivery`, `cron-jobs` | cron jobs advisory lock: `~/.hermes/cron/.jobs.lock`. The prompt explicitly forbids config, skill, cron, and ClickUp writes; scheduler-only job metadata remains serialized by the cron lock. | `Digest email and sender receipt`. No Hermes-owned production resource is changed by the digest; delivery correction is a subsequent digest or sender retry. | — |
| `repo-maintenance-cron` — Repository maintenance cron | `cron` | `hermes-agent` | `mutating` | `cron/scheduler.py`<br>`machine-setup/fleet-config/jobs.json`<br>Cron IDs: `59bdd8ebc468` | `repo-worktrees`, `cron-jobs` | cron jobs advisory lock and script-local deletion guards: `~/.hermes/cron/.jobs.lock`. Scheduler serializes metadata; each cleanup script owns its dry-run and deletion eligibility checks. | `Git worktree/ref recovery and retained .bak files`. Deletion is deliberately safety-gated; recover a removed worktree from its retained repository ref or backup rather than forcing a script past its guard. | — |
| `gateway-kanban-dispatcher` — Gateway Kanban dispatcher | `gateway` | `hermes-agent` | `mutating` | `gateway/kanban_watchers.py` | `kanban-dispatcher`, `session-db` | machine-global advisory lock and SQLite transactions: `~/.hermes/kanban/kanban/.dispatcher.lock`. Only the lock holder dispatches; board and session writes use their SQLite transaction contracts. | `Kanban event log and SQLite backups`. Stop the competing gateway, reconcile from event history, and restore only a verified database backup when lifecycle recovery requires it. | — |
| `dashboard-session-writer` — Dashboard session writer | `dashboard` | `hermes-agent` | `mutating` | `hermes_state.py`<br>`gateway/kanban_watchers.py` | `session-db` | SQLite WAL transaction: `~/.hermes/state.db`. Session DB writers are WAL multi-writer tolerant, subject to the lifecycle contract tracked by 86e2kmud2 rather than a fleet singleton. | `SQLite backup and session lifecycle records`. Recover using the session DB lifecycle procedure; never treat a WAL-capable session store as a fleet-wide single-writer resource. | — |
| `purelymail-poller-cron` — PurelyMail notify-me poller | `cron` | `ignite-email-infra` | `mutating` | `~/.hermes/scripts/purelymail-notify-poller.py`<br>`ignite-email-infra: deploy-poller.sh (external repository)`<br>Cron IDs: `6e25865a22a4` | `purelymail-poller-deploy`, `cron-jobs`, `governed-mini-scripts` | cron jobs advisory lock and deployed poller manifest verification: `~/.hermes/cron/.jobs.lock`. The no-agent cron runs the hash-verified poller script installed by ignite-email-infra; scheduler serialization uses the cron jobs lock. | `~/.hermes/deploy/purelymail-poller/ previous deployed artifact and cron output`. Restore the prior verified poller release through ignite-email-infra deploy rollback; do not hand-edit live poller bytes under ~/.hermes/scripts/. | — |
| `ignite-email-infra-poller-deploy` — PurelyMail poller deploy | `ignite-email-infra-deploy` | `ignite-email-infra` | `mutating` | `ignite-email-infra: deploy-poller.sh (external repository)` | `purelymail-poller-deploy` | external deploy lock: `~/.hermes/deploy/purelymail-poller/.lock`. The external deployer serializes its own atomically staged poller deployment under the named lock. | `~/.hermes/deploy/purelymail-poller/ previous deployed artifact`. The owning ignite-email-infra deploy procedure selects the prior verified poller artifact; Hermes must not mutate this external actor directly. | — |

## Notes

- **Ignite skills synchronization:** The execution brief uses the historic sync_ignite_skills_to_hermes.sh name. The currently deployed governed entry point is ignite-skills-pull.sh.
- **ClickUp review SLA watchdog:** The fleet jobs file references the deployed Mini script; no tracked source file with this exact name exists in this checkout.
- **Stranded claim reaper:** The fleet jobs file references the deployed Mini script; no tracked source file with this exact name exists in this checkout.
- **Ignite board synchronization:** The fleet jobs file references the deployed Mini script; no tracked source file with this exact name exists in this checkout.
- **Fleet health digest cron:** Email delivery is an external side effect, but this actor is read-only for Hermes production resources.
- **PurelyMail notify-me poller:** Fleet jobs.json registers the Hermes cron schedule (*/45 * * * *); script bytes and deploy contract remain owned by ignite-email-infra.
- **PurelyMail poller deploy:** Cross-repository actor: recorded here because it writes Hermes paths, but implemented and tested in ignite-email-infra.
