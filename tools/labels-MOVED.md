# Label taxonomy moved

The canonical label registry lives at `ignitemarketing/hermes-ops` → `tools/labels/`.
It defines 16 labels across lifecycle / `lane:` / `model:` / `risk:` / `flags:`.

Do not create a second copy here.
